#include <stdio.h>
#include <stdio.h>
#include <sdlib.h>

int main(int argc, char** argv)
{
    if(1)
        printf("hello world!");
    if(2)
    {
        printf("hello world!");
        return 0;
    }
	printf("hello, world!");
	return 0;
}
